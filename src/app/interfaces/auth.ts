export interface LoginData{
    restaurantName:string,
    password:string,
}
export interface RegisterData {
  restaurantName: string;
  password: string;
  firstName: string;
  lastName: string;
  address: string;
  phoneNumber: string;
}
